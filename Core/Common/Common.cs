namespace Core.Common {
    public class Common {
        public enum ControlType {
            TextboxRadioList,
            CheckboxList,
            DropDownList,
            Editor,
            FileUpload,
            DatePicker,
            TimePicker,
            DateRangePicker,
            CityStateZip,
            Address,
            ImagePicker,
            VideoPicker,
            TreeSelector,
            ZipCodePicker,
            StatePicker,
            CityPicker
        }
    }
}
